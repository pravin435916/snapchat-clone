/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['avatars.githubusercontent.com'],
        // domains: ['avatars.googleusercontent.com'], // Add the hostname
      },
};
export default nextConfig;
